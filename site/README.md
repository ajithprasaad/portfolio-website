# site/ — owned by code-agent

The deliverable.

- `index.html` — the single-file portfolio (Tailwind via CDN).
- `screenshots/` — Puppeteer captures from the `visual-qa` loop.

Open `index.html` directly in a browser to preview, or deploy the file to any static
host (Vercel, Netlify, GitHub Pages).
